version: '3'

services:
  web-server:
    container_name: web_server
    build: ./frontend
    volumes:
      - ./frontend/src:/srv/gui
    ports:
      - '5001:5001'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    restart: always
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example